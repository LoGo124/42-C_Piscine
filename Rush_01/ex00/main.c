#include <unistd.h>

int main(int n_args, char param[])
{
	char *param_ptr;

	if (n_args != 2)
		return 0;
	param_ptr = &param[0];
}

int read_param(char *ptr)
{
	char a;

	a = *ptr
	while (a != '\0')
	{
		
	}
	
}